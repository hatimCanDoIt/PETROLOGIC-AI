/** Shared lith keys for canvas encoding (values come from palette at runtime). */
export const LITH_LABELS: Record<number, string> = {
  0: 'sandstone',
  1: 'dolomite',
  2: 'limestone',
  3: 'uncertain',
}

export const PX_PER_FT = 4 // baseline pixels per foot at zoomFactor=1
